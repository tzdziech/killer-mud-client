<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:MudClient.App.ViewModels"
             xmlns:models="using:MudClient.App.Models"
             xmlns:controls="using:MudClient.App.Controls"
             x:Name="AutomationRoot"
             x:Class="MudClient.App.Views.Panels.AutomationPanelView"
             x:DataType="vm:MainWindowViewModel">
  <UserControl.Resources>
    <!-- Leaf template for a timer inside the folder tree. -->
    <DataTemplate x:Key="TimerItemTemplate" x:DataType="models:TimerEntry">
      <Border Classes="mud-section" Padding="10,8" Margin="0,2">
        <StackPanel Spacing="6">

          <Grid ColumnDefinitions="*,Auto,Auto,Auto,Auto" ColumnSpacing="6">
            <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="6"
                        VerticalAlignment="Center">
              <TextBlock Text="{Binding Name}"
                         VerticalAlignment="Center"
                         TextTrimming="CharacterEllipsis" />
              <Border Classes="mud-badge mud-badge-gold">
                <TextBlock Text="{Binding IntervalText, StringFormat='co {0}'}" />
              </Border>
              <Border Classes="mud-badge" IsVisible="{Binding IsGlobal}"
                      ToolTip.Tip="Wspólny dla wszystkich kont">
                <TextBlock Text="Globalny" />
              </Border>
            </StackPanel>

            <ToggleButton Grid.Column="1" Classes="mud-switch"
                          IsChecked="{Binding IsEnabled, Mode=OneWay}"
                          Content="{Binding StatusText}"
                          Click="ToggleTimer_OnClick"
                          ToolTip.Tip="Włącz / wyłącz timer" />

            <Button Grid.Column="2"
                    Classes="mud-small"
                    Content="✎" Padding="6,2"
                    ToolTip.Tip="Edytuj timer"
                    Click="EditTimer_OnClick" />

            <Button Grid.Column="3"
                    Classes="mud-small"
                    Content="⇩" Padding="6,2"
                    ToolTip.Tip="Eksportuj timer do JSON"
                    Click="ExportItem_OnClick" />

            <Button Grid.Column="4"
                    Classes="mud-small mud-danger"
                    Content="✕" Padding="6,2"
                    ToolTip.Tip="Usuń timer"
                    Click="DeleteTimer_OnClick" />
          </Grid>

          <Border Classes="mud-inset" Padding="8,5">
            <TextBlock Text="{Binding CommandsText}"
                       Foreground="{StaticResource MudBrushParchmentDim}"
                       TextWrapping="Wrap" />
          </Border>

        </StackPanel>
      </Border>
    </DataTemplate>

    <!-- Shared item template for aliases and triggers (rendered in two sections). -->
    <DataTemplate x:Key="RuleItemTemplate" x:DataType="models:AutomationRuleEntry">
      <Border Classes="mud-section" Padding="10,8" Margin="0,2">
        <StackPanel Spacing="4">

          <Grid ColumnDefinitions="*,Auto,Auto,Auto,Auto,Auto" ColumnSpacing="6">
            <TextBlock Grid.Column="0" Text="{Binding Name}"
                       FontWeight="SemiBold"
                       Foreground="{StaticResource MudBrushParchment}"
                       VerticalAlignment="Center"
                       TextTrimming="CharacterEllipsis" />
            <Border Grid.Column="1" Classes="mud-badge mud-badge-gold" IsVisible="{Binding IsGlobal}"
                    ToolTip.Tip="Wspólna dla wszystkich kont">
              <TextBlock Text="Globalna" />
            </Border>
            <ToggleButton Grid.Column="2" Classes="mud-switch"
                          IsChecked="{Binding IsEnabled, Mode=OneWay}"
                          Content="{Binding StatusText}"
                          Click="ToggleRule_OnClick"
                          ToolTip.Tip="Włącz / wyłącz regułę" />
            <Button Grid.Column="3"
                    Classes="mud-small"
                    Content="✎" Padding="6,2"
                    ToolTip.Tip="Edytuj regułę"
                    Click="EditRule_OnClick" />
            <Button Grid.Column="4"
                    Classes="mud-small"
                    Content="⇩" Padding="6,2"
                    ToolTip.Tip="Eksportuj wpis do JSON"
                    Click="ExportItem_OnClick" />
            <Button Grid.Column="5"
                    Classes="mud-small mud-danger"
                    Content="✕" Padding="6,2"
                    ToolTip.Tip="Usuń regułę"
                    Click="DeleteRule_OnClick" />
          </Grid>

          <Grid ColumnDefinitions="Auto,*" ColumnSpacing="6" RowDefinitions="Auto,Auto" RowSpacing="1">
            <TextBlock Grid.Row="0" Grid.Column="0" Classes="mud-faint" Text="Wzorzec" />
            <TextBlock Grid.Row="0" Grid.Column="1" Classes="mud-faint"
                       Text="{Binding Pattern}" TextTrimming="CharacterEllipsis"
                       ToolTip.Tip="{Binding Pattern}" />
            <TextBlock Grid.Row="1" Grid.Column="0" Classes="mud-faint" Text="Akcja" />
            <TextBlock Grid.Row="1" Grid.Column="1" Classes="mud-faint"
                       Text="{Binding Action}" TextWrapping="Wrap" />
          </Grid>

        </StackPanel>
      </Border>
    </DataTemplate>

    <DataTemplate x:Key="TimerEditorTemplate" x:DataType="vm:MainWindowViewModel">
      <StackPanel Spacing="8">
        <TextBox Text="{Binding NewTimerName}" PlaceholderText="Nazwa timera…" />
        <StackPanel Spacing="3">
          <TextBlock Classes="mud-faint" Text="Interwał powtarzania" />
          <Grid ColumnDefinitions="*,*,*" ColumnSpacing="6">
            <TextBox Grid.Column="0" Text="{Binding NewTimerMinutes}" PlaceholderText="0" InnerRightContent="min" />
            <TextBox Grid.Column="1" Text="{Binding NewTimerSeconds}" PlaceholderText="0" InnerRightContent="s" />
            <TextBox Grid.Column="2" Text="{Binding NewTimerMilliseconds}" PlaceholderText="0" InnerRightContent="ms" />
          </Grid>
        </StackPanel>
        <StackPanel Spacing="3">
          <TextBlock Classes="mud-faint" Text="Komendy — każda w osobnej linii" />
          <TextBox Text="{Binding NewTimerCommands}"
                   PlaceholderText="np.&#x0a;rzuc 'leczenie'&#x0a;pij miksture"
                   MinHeight="76" AcceptsReturn="True" TextWrapping="Wrap" />
        </StackPanel>
        <CheckBox IsChecked="{Binding NewTimerIsGlobal}"
                  Content="Globalny — dostępny na wszystkich kontach" />
        <Grid ColumnDefinitions="*,Auto" ColumnSpacing="6">
          <Button Content="{Binding TimerFormButtonText}" Command="{Binding AddTimerCommand}"
                  HorizontalAlignment="Stretch" HorizontalContentAlignment="Center" />
          <Button Grid.Column="1" Content="Anuluj" Command="{Binding CancelTimerEditCommand}" />
        </Grid>
      </StackPanel>
    </DataTemplate>

    <DataTemplate x:Key="RuleEditorTemplate" x:DataType="vm:MainWindowViewModel">
      <StackPanel Spacing="8">
        <TextBox Text="{Binding NewRuleName}" PlaceholderText="Nazwa…" />
        <StackPanel Spacing="3">
          <TextBlock Classes="mud-faint" Text="Wzorzec (regex .NET)" />
          <TextBox Text="{Binding NewRulePattern}"
                   PlaceholderText="np. ^zb (.+)$  albo  (\w+) upada na ziemie" />
          <TextBlock Text="{Binding NewRulePatternError}"
                     Foreground="{StaticResource MudBrushCrimsonBright}"
                     TextWrapping="Wrap" IsVisible="{Binding HasNewRulePatternError}" />
        </StackPanel>
        <StackPanel Spacing="3">
          <TextBlock Classes="mud-faint" Text="Akcja — komendy w osobnych liniach; grupy: $1, $2…" />
          <TextBox Text="{Binding NewRuleAction}"
                   PlaceholderText="np.&#x0a;rzuc 'leczenie' $1&#x0a;pij miksture"
                   MinHeight="76" AcceptsReturn="True" TextWrapping="Wrap" />
        </StackPanel>
        <CheckBox IsChecked="{Binding NewRuleIsGlobal}"
                  Content="Globalny — dostępny na wszystkich kontach" />
        <Grid ColumnDefinitions="*,Auto" ColumnSpacing="6">
          <Button Content="{Binding RuleFormButtonText}" Command="{Binding AddRuleCommand}"
                  HorizontalAlignment="Stretch" HorizontalContentAlignment="Center" />
          <Button Grid.Column="1" Content="Anuluj" Command="{Binding CancelRuleEditCommand}" />
        </Grid>
      </StackPanel>
    </DataTemplate>
  </UserControl.Resources>
  <TabControl Classes="mud-subtabs mud-automation-tabs"
              SelectedIndex="{Binding SelectedAutomationTabIndex, Mode=TwoWay}">
    <TabItem>
      <TabItem.Header>
        <StackPanel Orientation="Horizontal" Spacing="6">
          <TextBlock Text="Timery" />
          <Border Classes="mud-badge"><TextBlock Text="{Binding Timers.Count}" /></Border>
        </StackPanel>
      </TabItem.Header>
      <Grid RowDefinitions="Auto,*">
        <Border Classes="mud-bar-bottom">
          <Grid ColumnDefinitions="*,Auto,Auto" ColumnSpacing="6">
            <Button Content="＋ Nowy timer" Command="{Binding StartAddTimerCommand}"
                    HorizontalAlignment="Stretch" HorizontalContentAlignment="Center" />
            <Button Grid.Column="1" Classes="mud-small" Content="📁 Folder"
                    ToolTip.Tip="Utwórz folder na timery"
                    Command="{Binding CreateFolderCommand}"
                    CommandParameter="{x:Static models:FolderKind.Timers}" />
            <Button Grid.Column="2" Classes="mud-small" Content="⇧ JSON"
                    ToolTip.Tip="Importuj timery lub folder z pliku JSON"
                    Tag="{x:Static models:FolderKind.Timers}" Click="Import_OnClick" />
          </Grid>
        </Border>
        <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Disabled">
          <StackPanel Spacing="8" Margin="10,8,10,12">

      <Expander Classes="mud-expander"
                Header="{Binding TimerFormHeader}"
                IsExpanded="{Binding IsTimerFormExpanded}"
                IsVisible="{Binding IsTimerFormExpanded}">
        <ContentControl Content="{Binding}"
                        ContentTemplate="{StaticResource TimerEditorTemplate}"
                        Margin="0,8,0,0" />
      </Expander>

      <TextBlock Classes="mud-faint" TextWrapping="Wrap"
                 Text="Brak timerów. Timer powtarza komendy w nieskończoność, dopóki go nie wyłączysz."
                 IsVisible="{Binding !Timers.Count}" />

      <controls:FolderTreeView Nodes="{Binding TimerTree}"
                               ItemTemplate="{StaticResource TimerItemTemplate}"
                               SupportsEnableToggle="True"
                               SupportsExport="True"
                               FolderExportRequested="FolderTree_OnFolderExportRequested"
                               MoveIntoFolderCommand="{Binding MoveIntoFolderCommand}"
                               CreateSubfolderCommand="{Binding CreateSubfolderCommand}"
                               RenameFolderCommand="{Binding RenameFolderCommand}"
                               DeleteFolderCommand="{Binding ConfirmDeleteFolderCommand, ElementName=AutomationRoot}"
                               ToggleFolderGlobalCommand="{Binding ToggleFolderGlobalCommand}"
                               ToggleFolderEnabledCommand="{Binding ToggleFolderEnabledCommand}" />

          </StackPanel>
        </ScrollViewer>
      </Grid>
    </TabItem>

    <TabItem>
      <TabItem.Header>
        <StackPanel Orientation="Horizontal" Spacing="6">
          <TextBlock Text="Aliasy" />
          <Border Classes="mud-badge"><TextBlock Text="{Binding AliasRules.Count}" /></Border>
        </StackPanel>
      </TabItem.Header>
      <Grid RowDefinitions="Auto,*">
        <Border Classes="mud-bar-bottom">
          <Grid ColumnDefinitions="*,Auto,Auto" ColumnSpacing="6">
            <Button Content="＋ Nowy alias" Command="{Binding StartAddAliasCommand}"
                    HorizontalAlignment="Stretch" HorizontalContentAlignment="Center" />
            <Button Grid.Column="1" Classes="mud-small" Content="📁 Folder"
                    ToolTip.Tip="Utwórz folder na aliasy"
                    Command="{Binding CreateFolderCommand}"
                    CommandParameter="{x:Static models:FolderKind.Aliases}" />
            <Button Grid.Column="2" Classes="mud-small" Content="⇧ JSON"
                    ToolTip.Tip="Importuj alias lub folder z pliku JSON"
                    Tag="{x:Static models:FolderKind.Aliases}" Click="Import_OnClick" />
          </Grid>
        </Border>
        <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Disabled">
          <StackPanel Spacing="8" Margin="10,8,10,12">
            <Expander Classes="mud-expander"
                      Header="{Binding RuleFormHeader}"
                      IsExpanded="{Binding IsRuleFormExpanded}"
                      IsVisible="{Binding IsAliasRuleFormVisible}">
              <ContentControl Content="{Binding}"
                              ContentTemplate="{StaticResource RuleEditorTemplate}"
                              Margin="0,8,0,0" />
            </Expander>

      <TextBlock Classes="mud-faint" TextWrapping="Wrap"
                 Text="Brak aliasów. Alias podmienia komendę, którą wpisujesz (np. skrót)."
                 IsVisible="{Binding !AliasRules.Count}" />

      <controls:FolderTreeView Nodes="{Binding AliasTree}"
                               ItemTemplate="{StaticResource RuleItemTemplate}"
                               SupportsEnableToggle="True"
                               SupportsExport="True"
                               FolderExportRequested="FolderTree_OnFolderExportRequested"
                               MoveIntoFolderCommand="{Binding MoveIntoFolderCommand}"
                               CreateSubfolderCommand="{Binding CreateSubfolderCommand}"
                               RenameFolderCommand="{Binding RenameFolderCommand}"
                               DeleteFolderCommand="{Binding ConfirmDeleteFolderCommand, ElementName=AutomationRoot}"
                               ToggleFolderGlobalCommand="{Binding ToggleFolderGlobalCommand}"
                               ToggleFolderEnabledCommand="{Binding ToggleFolderEnabledCommand}" />

          </StackPanel>
        </ScrollViewer>
      </Grid>
    </TabItem>

    <TabItem>
      <TabItem.Header>
        <StackPanel Orientation="Horizontal" Spacing="6">
          <TextBlock Text="Triggery" />
          <Border Classes="mud-badge"><TextBlock Text="{Binding TriggerRules.Count}" /></Border>
        </StackPanel>
      </TabItem.Header>
      <Grid RowDefinitions="Auto,*">
        <Border Classes="mud-bar-bottom">
          <Grid ColumnDefinitions="*,Auto,Auto" ColumnSpacing="6">
            <Button Content="＋ Nowy trigger" Command="{Binding StartAddTriggerCommand}"
                    HorizontalAlignment="Stretch" HorizontalContentAlignment="Center" />
            <Button Grid.Column="1" Classes="mud-small" Content="📁 Folder"
                    ToolTip.Tip="Utwórz folder na triggery"
                    Command="{Binding CreateFolderCommand}"
                    CommandParameter="{x:Static models:FolderKind.Triggers}" />
            <Button Grid.Column="2" Classes="mud-small" Content="⇧ JSON"
                    ToolTip.Tip="Importuj trigger lub folder z pliku JSON"
                    Tag="{x:Static models:FolderKind.Triggers}" Click="Import_OnClick" />
          </Grid>
        </Border>
        <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Disabled">
          <StackPanel Spacing="8" Margin="10,8,10,12">
            <Expander Classes="mud-expander"
                      Header="{Binding RuleFormHeader}"
                      IsExpanded="{Binding IsRuleFormExpanded}"
                      IsVisible="{Binding IsTriggerRuleFormVisible}">
              <ContentControl Content="{Binding}"
                              ContentTemplate="{StaticResource RuleEditorTemplate}"
                              Margin="0,8,0,0" />
            </Expander>

      <TextBlock Classes="mud-faint" TextWrapping="Wrap"
                 Text="Brak triggerów. Trigger wysyła akcję, gdy linia z serwera pasuje do wzorca."
                 IsVisible="{Binding !TriggerRules.Count}" />

      <controls:FolderTreeView Nodes="{Binding TriggerTree}"
                               ItemTemplate="{StaticResource RuleItemTemplate}"
                               SupportsEnableToggle="True"
                               SupportsExport="True"
                               FolderExportRequested="FolderTree_OnFolderExportRequested"
                               MoveIntoFolderCommand="{Binding MoveIntoFolderCommand}"
                               CreateSubfolderCommand="{Binding CreateSubfolderCommand}"
                               RenameFolderCommand="{Binding RenameFolderCommand}"
                               DeleteFolderCommand="{Binding ConfirmDeleteFolderCommand, ElementName=AutomationRoot}"
                               ToggleFolderGlobalCommand="{Binding ToggleFolderGlobalCommand}"
                               ToggleFolderEnabledCommand="{Binding ToggleFolderEnabledCommand}" />

          </StackPanel>
        </ScrollViewer>
      </Grid>
    </TabItem>

    <TabItem Header="Drużyna">
      <ScrollViewer HorizontalScrollBarVisibility="Disabled">
        <StackPanel Spacing="8" Margin="10,8,10,12">
          <TextBlock Classes="mud-heading" Text="Automaty drużynowe" />
          <TextBlock Classes="mud-faint" TextWrapping="Wrap"
                     Text="Automatyczne reakcje na stan drużyny i komunikaty z gry." />

          <Border Classes="mud-section" Padding="10">
            <StackPanel Spacing="10">
              <StackPanel Spacing="3">
                <CheckBox IsChecked="{Binding AutoAssistEnabled}"
                          Content="Autoassist — automatyczne wspieranie drużyny" />
                <TextBlock Classes="mud-faint" TextWrapping="Wrap"
                           Text="Gdy GMCP wykryje, że inny członek drużyny walczy w tym samym pokoju, klient wyśle „as”. Jeśli przestaniesz walczyć, a członek drużyny nadal walczy, komenda zostanie ponowiona." />
                <TextBlock Margin="0,6,0,0"
                           Text="Komendy po asyście" />
                <TextBox MinHeight="64"
                         AcceptsReturn="True"
                         TextWrapping="Wrap"
                         PlaceholderText="Np. wesprzyj;czar 'ochrona'"
                         Text="{Binding AutoAssistFollowUpCommands, UpdateSourceTrigger=PropertyChanged}" />
                <TextBlock Classes="mud-faint" TextWrapping="Wrap"
                           Text="Wysyłane po komendzie „as”. Możesz wpisać wiele wierszy lub rozdzielić komendy separatorem ustawionym w opcjach klienta." />
                <TextBlock Margin="0,6,0,0"
                           Text="Moby wykluczone z autoassista" />
                <TextBox MinHeight="64"
                         AcceptsReturn="True"
                         TextWrapping="Wrap"
                         PlaceholderText="Dokładna nazwa moba — po jednej w wierszu"
                         Text="{Binding AutoAssistExcludedMobNamesText, UpdateSourceTrigger=PropertyChanged}" />
                <TextBlock Classes="mud-faint" TextWrapping="Wrap"
                           Text="Klient nie wyśle „as”, gdy członek grupy w aktualnym pokoju walczy z mobem o jednej z tych nazw. Dopasowanie obejmuje całą nazwę i nie rozróżnia wielkości liter." />
              </StackPanel>

              <StackPanel Spacing="3">
                <CheckBox IsChecked="{Binding AutoAssistNpcEnabled}"
                          Content="Autoassist NPC — rozkaż własnemu summonowi wspierać w walce" />
                <TextBlock Classes="mud-faint" TextWrapping="Wrap"
                           Text="Gdy Twoja pozycja GMCP zmieni się na „walczy”, klient wyśle „order &lt;NPC&gt; assist” do każdego NPC w aktualnej grupie (np. przywołanego/zaczarowanego summona). Nie wymaga bycia liderem grupy." />
              </StackPanel>

              <StackPanel Spacing="3">
                <CheckBox IsChecked="{Binding GroupOrdersEnabled}"
                          Content="Ordery — wykonuj rozkazy członków drużyny" />
                <TextBlock Classes="mud-faint" TextWrapping="Wrap"
                           Text="Klient wykona komendę z komunikatu „Gracz rozkazuje ci 'komenda'.” tylko wtedy, gdy Gracz znajduje się w aktualnej grupie GMCP." />
              </StackPanel>

              <StackPanel Spacing="3">
                <CheckBox IsChecked="{Binding AutoRecastOnLeaderSnapEnabled}"
                          Content="Autorecast — reaguj na pstryknięcie palcami lidera" />
                <TextBlock Classes="mud-faint" TextWrapping="Wrap"
                           Text="Gdy lider aktualnej grupy GMCP pstryknie palcami, klient wyśle poniższe komendy." />
                <TextBlock Margin="0,6,0,0"
                           Text="Komendy po pstryknięciu" />
                <TextBox MinHeight="64"
                         AcceptsReturn="True"
                         TextWrapping="Wrap"
                         PlaceholderText="Np. /recast"
                         Text="{Binding AutoRecastOnLeaderSnapCommandsText, UpdateSourceTrigger=PropertyChanged}" />
                <TextBlock Classes="mud-faint" TextWrapping="Wrap"
                           Text="Możesz wpisać wiele wierszy lub rozdzielić komendy separatorem ustawionym w opcjach klienta." />
              </StackPanel>
            </StackPanel>
          </Border>
        </StackPanel>
      </ScrollViewer>
    </TabItem>

    <TabItem Header="Podróż">
      <ScrollViewer HorizontalScrollBarVisibility="Disabled">
        <StackPanel Spacing="8" Margin="10,8,10,12">
          <TextBlock Classes="mud-heading" Text="Autowalk — odzyskiwanie ruchu" />
          <TextBlock Classes="mud-faint" TextWrapping="Wrap"
                     Text="Przed każdym krokiem autowalk sprawdza punkty ruchu (MV). Gdy spadną do progu poniżej, rzuca refresh (jeśli zapamiętany), a w przeciwnym razie odpoczywa przez ustawiony czas — po czym trasa wznawia się sama." />

          <Border Classes="mud-section" Padding="10">
            <StackPanel Spacing="10">
              <StackPanel Spacing="3">
                <CheckBox IsChecked="{Binding AutowalkMovementRecoveryEnabled}"
                          Content="Automatyczne odzyskiwanie ruchu podczas autowalk" />
                <TextBlock Classes="mud-faint" TextWrapping="Wrap"
                           Text="Wyłączenie sprawia, że autowalk idzie dalej bez sprawdzania punktów ruchu — w trasie można utknąć bez MV." />
              </StackPanel>

              <StackPanel Spacing="3" IsEnabled="{Binding AutowalkMovementRecoveryEnabled}">
                <Grid ColumnDefinitions="*,Auto">
                  <TextBlock Classes="mud-label" Text="Próg niskiego ruchu" />
                  <Border Grid.Column="1" Classes="mud-badge mud-badge-gold">
                    <TextBlock Text="{Binding AutowalkLowMovementThresholdPercent, StringFormat='{}{0}%'}" />
                  </Border>
                </Grid>
                <Slider Minimum="{Binding MinAutowalkLowMovementThresholdPercent}"
                        Maximum="{Binding MaxAutowalkLowMovementThresholdPercent}"
                        Value="{Binding AutowalkLowMovementThresholdPercent}"
                        TickFrequency="1" IsSnapToTickEnabled="True" />
                <TextBlock Classes="mud-faint" TextWrapping="Wrap"
                           Text="Autowalk zaczyna odzyskiwać ruch, gdy aktualne MV spadną do tego procentu maksimum lub niżej." />

                <Grid ColumnDefinitions="*,Auto" Margin="0,6,0,0">
                  <TextBlock Classes="mud-label" Text="Czas odpoczynku" />
                  <Border Grid.Column="1" Classes="mud-badge mud-badge-gold">
                    <TextBlock Text="{Binding AutowalkRestSeconds, StringFormat='{}{0} s'}" />
                  </Border>
                </Grid>
                <Slider Minimum="{Binding MinAutowalkRestSeconds}"
                        Maximum="{Binding MaxAutowalkRestSeconds}"
                        Value="{Binding AutowalkRestSeconds}"
                        TickFrequency="5" IsSnapToTickEnabled="True" />
                <TextBlock Classes="mud-faint" TextWrapping="Wrap"
                           Text="Używane tylko, gdy refresh nie jest zapamiętany: klient wysyła rest, czeka tyle sekund, wstaje i wznawia trasę." />
              </StackPanel>
            </StackPanel>
          </Border>

          <TextBlock Classes="mud-heading" Text="Po dotarciu do celu" Margin="0,6,0,0" />
          <Border Classes="mud-section" Padding="10">
            <StackPanel Spacing="3">
              <CheckBox IsChecked="{Binding AutowalkRestOnArrivalEnabled}"
                        Content="Automatycznie odpocznij po dotarciu do lokacji" />
              <TextBlock Classes="mud-faint" TextWrapping="Wrap"
                         Text="Gdy autowalk dotrze do celu, klient wysyła „rest”. Wyłączenie zostawia postać w takiej pozycji, w jakiej dotarła." />
            </StackPanel>
          </Border>

          <TextBlock Classes="mud-heading" Text="Drużyna w podróży" Margin="0,6,0,0" />
          <Border Classes="mud-section" Padding="10">
            <StackPanel Spacing="10">
              <StackPanel Spacing="6">
                <TextBlock Classes="mud-faint" TextWrapping="Wrap"
                           Text="Wysyła „order &lt;gracz&gt; cast refresh” do każdego innego członka drużyny po kolei, żeby każdy rzucił refresh na siebie zamiast czekać, aż zrobisz to za niego." />
                <Button Content="Rozkaż drużynie rzucić refresh"
                        HorizontalAlignment="Stretch" HorizontalContentAlignment="Center"
                        Command="{Binding CastRefreshOnGroupCommand}" />
                <CheckBox IsChecked="{Binding AutoGroupRefreshOnExhaustedEnabled}"
                          Content="Auto — rozkaż refresh, gdy ktoś w drużynie jest zamęczony" />
                <TextBlock Classes="mud-faint" TextWrapping="Wrap"
                           Text="Gdy GMCP zgłosi czyjeś MV jako „zamęczony”, klient jednorazowo wyśle mu „order &lt;gracz&gt; cast refresh” — ponownie dopiero po odpoczynku i kolejnym wyczerpaniu." />
              </StackPanel>

              <Border Height="1" Background="{StaticResource MudBrushSlate}" Margin="0,4" />

              <StackPanel Spacing="3">
                <CheckBox IsChecked="{Binding AutoStandOrderEnabled}"
                          Content="Autostand — rozkaż drużynie wstać, gdy Ty wstajesz" />
                <CheckBox IsChecked="{Binding AutoRestOrderEnabled}"
                          Content="Autorest — rozkaż drużynie odpocząć, gdy Ty odpoczywasz" />
                <TextBlock Classes="mud-faint" TextWrapping="Wrap"
                           Text="Działa tylko, gdy jesteś liderem aktualnej grupy GMCP — wysyła „order &lt;gracz&gt; stand” po komendzie stand / „order &lt;gracz&gt; rest” po komendzie rest do każdego innego członka." />
              </StackPanel>

              <Border Height="1" Background="{StaticResource MudBrushSlate}" Margin="0,4" />

              <StackPanel Spacing="3">
                <CheckBox IsChecked="{Binding AutoFollowLeaderEnabled}"
                          Content="Autofollow — dogoń lidera, gdy jesteś w innym pokoju" />
                <TextBlock Classes="mud-faint" TextWrapping="Wrap"
                           Text="Dla członka drużyny (nie lidera): jeśli GMCP zgłosi lidera w innym pokoju niż Ty, klient sam wysyła to samo, co „/walk leader” — bez czekania na rozkaz od lidera. Nic nie robi, gdy już gdzieś idziesz (np. farma) albo walczysz." />
              </StackPanel>
            </StackPanel>
          </Border>
        </StackPanel>
      </ScrollViewer>
    </TabItem>

    <TabItem Header="Walka">
      <ScrollViewer HorizontalScrollBarVisibility="Disabled">
        <StackPanel Spacing="8" Margin="10,8,10,12">
          <TextBlock Classes="mud-heading" Text="Automatyczne reakcje w walce" />
          <TextBlock Classes="mud-faint" TextWrapping="Wrap"
                     Text="Twarde reakcje na konkretne sytuacje w walce — nie są to reguły triggerów, więc nie pojawiają się na liście Triggery." />

          <Border Classes="mud-section" Padding="10">
            <StackPanel Spacing="10">
              <StackPanel Spacing="3">
                <CheckBox IsChecked="{Binding AutoStandOnLyingEnabled}"
                          Content="Autostand — wstań, gdy zostaniesz powalony" />
                <TextBlock Classes="mud-faint" TextWrapping="Wrap"
                           Text="Gdy Twoja pozycja GMCP zmieni się na „leży”, albo pojawi się komunikat „powala cię na ziemię”/„przewracasz się”/„osuwasz się półprzytomny”, klient wyśle „stand”." />
              </StackPanel>

              <Border Height="1" Background="{StaticResource MudBrushSlate}" Margin="0,4" />

              <StackPanel Spacing="3">
                <CheckBox IsChecked="{Binding AutowieldEnabled}"
                          Content="Autowield — podnieś i załóż broń po rozbrojeniu" />
                <TextBlock Classes="mud-faint" TextWrapping="Wrap"
                           Text="Gdy pojawi się komunikat „rozbraja cię”, klient wyśle „get &lt;broń&gt;”, a potem „wield &lt;broń&gt;”." />
                <TextBlock Margin="0,6,0,0" Text="Nazwa broni" />
                <TextBox Text="{Binding AutowieldWeaponName, UpdateSourceTrigger=PropertyChanged}"
                         PlaceholderText="Np. miecz" />
                <TextBlock Classes="mud-faint" TextWrapping="Wrap"
                           Text="Dokładna nazwa Twojej broni, używana w komendach get/wield." />
              </StackPanel>
            </StackPanel>
          </Border>
        </StackPanel>
      </ScrollViewer>
    </TabItem>

    <TabItem Header="Farma">
      <ScrollViewer HorizontalScrollBarVisibility="Disabled">
        <StackPanel Spacing="8" Margin="10,8,10,12">
          <TextBlock Classes="mud-heading" Text="Auto-farma" />
          <TextBlock Classes="mud-faint" TextWrapping="Wrap"
                     Text="Chodzi po nieodwiedzonych pokojach w obszarze zaznaczonym na mapie (⚙ Ustawienia mapy → Obszar farmy), pozwalając Autokillowi (tam samo) walczyć po drodze. Gdy HP spadnie do progu poniżej, farma przerywa chodzenie i leczy się/odpoczywa zamiast iść dalej." />

          <Border Classes="mud-section" Padding="10">
            <StackPanel Spacing="10">
              <TextBlock Classes="mud-faint" TextWrapping="Wrap" Text="{Binding Map.AutoFarmRegionStatusText}" />
              <TextBlock Classes="mud-faint" TextWrapping="Wrap" Text="{Binding AutoFarmStatusText}" />
              <Grid ColumnDefinitions="*,*" ColumnSpacing="6">
                <Button Grid.Column="0" Content="Uruchom farmę"
                        HorizontalAlignment="Stretch" HorizontalContentAlignment="Center"
                        Command="{Binding StartAutoFarmCommand}" />
                <Button Grid.Column="1" Content="Zatrzymaj farmę"
                        HorizontalAlignment="Stretch" HorizontalContentAlignment="Center"
                        Command="{Binding StopAutoFarmCommand}" />
              </Grid>
            </StackPanel>
          </Border>

          <TextBlock Classes="mud-heading" Text="Leczenie i odpoczynek" Margin="0,6,0,0" />
          <Border Classes="mud-section" Padding="10">
            <StackPanel Spacing="10">
              <StackPanel Spacing="3">
                <Grid ColumnDefinitions="*,Auto">
                  <TextBlock Classes="mud-label" Text="Próg HP" />
                  <Border Grid.Column="1" Classes="mud-badge mud-badge-gold">
                    <TextBlock Text="{Binding AutoFarmHpThresholdPercent, StringFormat='{}{0}%'}" />
                  </Border>
                </Grid>
                <Slider Minimum="{Binding MinAutoFarmHpThresholdPercent}"
                        Maximum="{Binding MaxAutoFarmHpThresholdPercent}"
                        Value="{Binding AutoFarmHpThresholdPercent}"
                        TickFrequency="5" IsSnapToTickEnabled="True" />
                <TextBlock Classes="mud-faint" TextWrapping="Wrap"
                           Text="Farma przerywa chodzenie i zaczyna się leczyć, gdy aktualne HP spadnie do tego procentu maksimum lub niżej." />
              </StackPanel>

              <StackPanel Spacing="3">
                <TextBlock Classes="mud-label" Text="Zaklęcie leczące (rzucane na siebie)" />
                <TextBox Text="{Binding AutoFarmHealSpellName, UpdateSourceTrigger=PropertyChanged}"
                         PlaceholderText="Np. heal — puste pole = tylko odpoczynek" />
                <TextBlock Classes="mud-faint" TextWrapping="Wrap"
                           Text="Gdy HP spadnie poniżej progu: jeśli zaklęcie jest zapamiętane, farma je rzuca; jeśli nie, najpierw je memuje — a przy okazji zawsze też odpoczywa (używa czasu odpoczynku ustawionego w zakładce „Podróż”)." />
              </StackPanel>

              <StackPanel Spacing="3">
                <TextBlock Classes="mud-label" Text="Zaklęcia zawsze zapamiętane" />
                <TextBox Text="{Binding AutoFarmRequiredMemorizedSpellsText, UpdateSourceTrigger=PropertyChanged}"
                         AcceptsReturn="True"
                         Height="70"
                         TextWrapping="Wrap"
                         PlaceholderText="Jedno zaklęcie na linię, np. armor&#x0a;bless" />
                <TextBlock Classes="mud-faint" TextWrapping="Wrap"
                           Text="Farma sprawdza to razem z progiem HP — jeśli któregoś z tych zaklęć zabraknie (nie jest zapamiętane), przerywa chodzenie, memuje brakujące i odpoczywa, tak samo jak przy zaklęciu leczącym." />
              </StackPanel>
            </StackPanel>
          </Border>
        </StackPanel>
      </ScrollViewer>
    </TabItem>
  </TabControl>
</UserControl>
