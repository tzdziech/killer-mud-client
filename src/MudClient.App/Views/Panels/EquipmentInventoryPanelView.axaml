<UserControl xmlns="https://github.com/avaloniaui" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:controls="using:MudClient.App.Controls" xmlns:equipment="using:MudClient.Core.Equipment" xmlns:gmcp="using:MudClient.Core.Gmcp" xmlns:vm="using:MudClient.App.ViewModels" x:Class="MudClient.App.Views.Panels.EquipmentInventoryPanelView" x:Name="EquipmentInventoryPanel" x:DataType="vm:MainWindowViewModel">
  <Grid RowDefinitions="Auto,*" RowSpacing="4">
    <CheckBox Grid.Row="0" Content="Monitorowanie ekwipunku (eksperymentalny)" IsChecked="{Binding EquipmentMonitoringEnabled}" Margin="6,4,6,0" />
  <TabControl Grid.Row="1" Classes="mud-subtabs" IsEnabled="{Binding EquipmentMonitoringEnabled}">
    <TabItem Header="Equipment"><ScrollViewer><StackPanel Margin="6" Spacing="2">
      <TextBlock Text="{Binding EquipmentInventory.StatusText}" Classes="mud-faint" FontSize="11"/>
      <ItemsControl ItemsSource="{Binding EquipmentInventory.Equipment}"><ItemsControl.ItemTemplate><DataTemplate x:DataType="vm:EquipmentInventoryRow">
        <Border Classes="mud-section" Padding="4" Margin="0,1"><ToolTip.Tip><TextBlock Text="{Binding ExamineDescription}" TextWrapping="NoWrap"/></ToolTip.Tip><Border.ContextMenu><ContextMenu><MenuItem Header="Zbadaj" Command="{Binding #EquipmentInventoryPanel.DataContext.ExecuteEquipmentExamineCommand}" CommandParameter="{Binding}"/><Separator/><MenuItem Header="Zdejmij" Command="{Binding #EquipmentInventoryPanel.DataContext.ExecuteEquipmentRemoveCommand}" CommandParameter="{Binding}"/><Separator/><MenuItem Header="Wstaw nazwę do linii poleceń" Command="{Binding #EquipmentInventoryPanel.DataContext.InsertEquipmentItemNameToCommandBarCommand}" CommandParameter="{Binding}"/></ContextMenu></Border.ContextMenu><Grid ColumnDefinitions="150,Auto,*,Auto"><TextBlock Text="{Binding Location}" Classes="mud-faint" FontSize="11"/><TextBlock Grid.Column="1" Text="{Binding DurabilityWarningText}" Classes="mud-status-bad" FontSize="11" Margin="0,0,5,0"/><controls:AnsiTextBlock Grid.Column="2" AnsiText="{Binding Name}" TextWrapping="Wrap" FontSize="12"/><TextBlock Grid.Column="3" Text="{Binding DurabilityWarningText}" Classes="mud-status-bad" FontSize="11" Margin="5,0,0,0"/></Grid></Border>
      </DataTemplate></ItemsControl.ItemTemplate></ItemsControl>
      <Border Classes="mud-section" Padding="5" Margin="0,4,0,0"><StackPanel Spacing="2"><TextBlock Text="Suma rozpoznanych bonusów" Classes="mud-faint" FontSize="11"/><TextBlock Text="{Binding EquipmentInventory.BonusSummaryText}" TextWrapping="Wrap" FontSize="12"/><ItemsControl ItemsSource="{Binding EquipmentInventory.BonusSummary}"><ItemsControl.ItemTemplate><DataTemplate x:DataType="vm:EquipmentBonusSummaryRow"><StackPanel Spacing="1"><TextBlock Text="{Binding Category}" Classes="mud-faint" FontSize="11" Margin="0,3,0,0" IsVisible="{Binding HasCategory}"/><TextBlock Text="{Binding Text}" ToolTip.Tip="{Binding Sources}" FontSize="12"/></StackPanel></DataTemplate></ItemsControl.ItemTemplate></ItemsControl></StackPanel></Border>
    </StackPanel></ScrollViewer></TabItem>
    <TabItem Header="Inventory"><ScrollViewer><StackPanel Margin="6" Spacing="2">
      <TextBlock Text="{Binding EquipmentInventory.StatusText}" Classes="mud-faint" FontSize="11"/>
      <ItemsControl ItemsSource="{Binding EquipmentInventory.Inventory}"><ItemsControl.ItemTemplate><DataTemplate x:DataType="vm:EquipmentInventoryRow">
        <Border Classes="mud-section" Padding="4" Margin="0,1">
          <ToolTip.Tip><TextBlock Text="{Binding ExamineDescription}" TextWrapping="NoWrap"/></ToolTip.Tip>
          <Border.ContextMenu><ContextMenu>
            <MenuItem Header="Zbadaj" Command="{Binding #EquipmentInventoryPanel.DataContext.ExecuteInventoryExamineCommand}" CommandParameter="{Binding}"/>
            <Separator/>
            <MenuItem Header="Identyfikacja u specjalisty" Command="{Binding #EquipmentInventoryPanel.DataContext.ExecuteInventorySpecialistIdentifyCommand}" CommandParameter="{Binding}"/>
            <MenuItem Header="Identyfikuj" IsVisible="{Binding #EquipmentInventoryPanel.DataContext.CanIdentifyItemsWithSpell}" Command="{Binding #EquipmentInventoryPanel.DataContext.ExecuteInventorySpellIdentifyCommand}" CommandParameter="{Binding}"/>
            <MenuItem Header="Daj przedmiot" IsVisible="{Binding #EquipmentInventoryPanel.DataContext.HasInventoryGiveTargets}" Tag="{Binding}" ItemsSource="{Binding #EquipmentInventoryPanel.DataContext.InventoryGiveTargets}" Click="GiveInventoryItem_OnClick"><MenuItem.ItemTemplate><DataTemplate x:DataType="gmcp:RoomPerson"><MenuItem Header="{Binding Name}"/></DataTemplate></MenuItem.ItemTemplate></MenuItem>
            <MenuItem Header="Wyjmij" IsVisible="{Binding IsIdentifiedContainer}" Tag="{Binding}" ItemsSource="{Binding ContainerContentsOrEmpty}" Click="TakeContainerItem_OnClick"><MenuItem.ItemTemplate><DataTemplate x:DataType="vm:ContainerInventoryItem"><MenuItem Header="{Binding MenuDisplayName}" FontSize="10" Padding="6,1" MinHeight="0" Margin="0"/></DataTemplate></MenuItem.ItemTemplate></MenuItem>
            <MenuItem Header="Wyjmij grupowo" IsVisible="{Binding HasContainerBulkActions}" Tag="{Binding}" ItemsSource="{Binding ContainerBulkActionsOrEmpty}" Click="TakeContainerItemGroup_OnClick"><MenuItem.ItemTemplate><DataTemplate x:DataType="equipment:ItemBulkGroup"><MenuItem Header="{Binding DisplayName}" FontSize="10" Padding="6,1" MinHeight="0" Margin="0"/></DataTemplate></MenuItem.ItemTemplate></MenuItem>
            <MenuItem Header="Włóż do" Tag="{Binding}" ItemsSource="{Binding #EquipmentInventoryPanel.DataContext.InventoryContainers}" Click="PutInventoryItem_OnClick"><MenuItem.ItemTemplate><DataTemplate x:DataType="vm:EquipmentInventoryRow"><MenuItem Header="{Binding MenuDisplayName}" FontSize="10" Padding="6,1" MinHeight="0" Margin="0"/></DataTemplate></MenuItem.ItemTemplate></MenuItem>
            <MenuItem Header="Włóż grupowo do" IsVisible="{Binding HasBulkActions}" ItemsSource="{Binding BulkActionsOrEmpty}"><MenuItem.ItemTemplate><DataTemplate x:DataType="equipment:ItemBulkGroup"><MenuItem Header="{Binding DisplayName}" Tag="{Binding}" ItemsSource="{Binding #EquipmentInventoryPanel.DataContext.InventoryContainers}" Click="PutInventoryItemGroup_OnClick"><MenuItem.ItemTemplate><DataTemplate x:DataType="vm:EquipmentInventoryRow"><MenuItem Header="{Binding MenuDisplayName}" FontSize="10" Padding="6,1" MinHeight="0" Margin="0"/></DataTemplate></MenuItem.ItemTemplate></MenuItem></DataTemplate></MenuItem.ItemTemplate></MenuItem>
            <Separator/>
            <MenuItem Header="Załóż" Command="{Binding #EquipmentInventoryPanel.DataContext.ExecuteInventoryWearCommand}" CommandParameter="{Binding}"/>
            <MenuItem Header="Trzymaj" Command="{Binding #EquipmentInventoryPanel.DataContext.ExecuteInventoryHoldCommand}" CommandParameter="{Binding}"/>
            <MenuItem Header="Użyj jako broni" Command="{Binding #EquipmentInventoryPanel.DataContext.ExecuteInventoryWieldCommand}" CommandParameter="{Binding}"/>
            <MenuItem Header="Upuść" Command="{Binding #EquipmentInventoryPanel.DataContext.ExecuteInventoryDropCommand}" CommandParameter="{Binding}"/>
            <MenuItem Header="Upuść grupowo" IsVisible="{Binding HasBulkActions}" ItemsSource="{Binding BulkActionsOrEmpty}"><MenuItem.ItemTemplate><DataTemplate x:DataType="equipment:ItemBulkGroup"><MenuItem Header="{Binding DisplayName}" Command="{Binding #EquipmentInventoryPanel.DataContext.ExecuteInventoryBulkDropCommand}" CommandParameter="{Binding}"/></DataTemplate></MenuItem.ItemTemplate></MenuItem>
            <MenuItem Header="Sprzedaj" Command="{Binding #EquipmentInventoryPanel.DataContext.ExecuteInventorySellCommand}" CommandParameter="{Binding}"/>
            <MenuItem Header="Sprzedaj grupowo" IsVisible="{Binding HasBulkActions}" ItemsSource="{Binding BulkActionsOrEmpty}"><MenuItem.ItemTemplate><DataTemplate x:DataType="equipment:ItemBulkGroup"><MenuItem Header="{Binding DisplayName}" Command="{Binding #EquipmentInventoryPanel.DataContext.ExecuteInventoryBulkSellCommand}" CommandParameter="{Binding}"/></DataTemplate></MenuItem.ItemTemplate></MenuItem>
            <Separator/>
            <MenuItem Header="Wstaw nazwę do linii poleceń" Command="{Binding #EquipmentInventoryPanel.DataContext.InsertEquipmentItemNameToCommandBarCommand}" CommandParameter="{Binding}"/>
          </ContextMenu></Border.ContextMenu>
          <Grid ColumnDefinitions="Auto,Auto,*,Auto,Auto"><TextBlock Text="{Binding DurabilityWarningText}" Classes="mud-status-bad" FontSize="11" Margin="0,0,5,0"/><TextBlock Grid.Column="1" Text="{Binding ContainerLabel}" Classes="mud-faint" FontSize="10" Margin="0,0,5,0"/><controls:AnsiTextBlock Grid.Column="2" AnsiText="{Binding Name}" TextWrapping="Wrap" FontSize="12"/><TextBlock Grid.Column="3" Text="{Binding RandomSlotDisplayText}" Classes="mud-faint" FontSize="10" Margin="5,0,0,0" IsVisible="{Binding HasRandomSlotLabel}"/><TextBlock Grid.Column="4" Text="{Binding DurabilityWarningText}" Classes="mud-status-bad" FontSize="11" Margin="5,0,0,0"/></Grid>
        </Border>
      </DataTemplate></ItemsControl.ItemTemplate></ItemsControl>
    </StackPanel></ScrollViewer></TabItem>
    <TabItem Header="Pokój"><ScrollViewer><StackPanel Margin="6" Spacing="2">
      <TextBlock Text="Przedmioty wykryte po wejściu do pokoju lub po poleceniu look." Classes="mud-faint" FontSize="11"/>
      <TextBlock Text="Brak rozpoznanych przedmiotów na ziemi." Classes="mud-faint" FontSize="11" IsVisible="{Binding !EquipmentInventory.GroundItems.Count}"/>
      <ItemsControl ItemsSource="{Binding EquipmentInventory.GroundItems}"><ItemsControl.ItemTemplate><DataTemplate x:DataType="vm:EquipmentInventoryRow">
        <Border Classes="mud-section" Padding="4" Margin="0,1"><ToolTip.Tip><TextBlock Text="{Binding ExamineDescription}" TextWrapping="NoWrap"/></ToolTip.Tip><Border.ContextMenu><ContextMenu>
          <MenuItem Header="Zbadaj" Command="{Binding #EquipmentInventoryPanel.DataContext.ExecuteGroundExamineCommand}" CommandParameter="{Binding}"/>
          <MenuItem Header="Podnieś" Command="{Binding #EquipmentInventoryPanel.DataContext.ExecuteGroundTakeCommand}" CommandParameter="{Binding}"/>
          <MenuItem Header="Napij się" IsVisible="{Binding IsWaterSource}" Command="{Binding #EquipmentInventoryPanel.DataContext.ExecuteGroundDrinkCommand}" CommandParameter="{Binding}"/>
          <MenuItem Header="Napełnij bukłak" IsVisible="{Binding IsWaterSource}" Tag="{Binding}" ItemsSource="{Binding #EquipmentInventoryPanel.DataContext.InventoryFlasks}" Click="FillInventoryFlask_OnClick"><MenuItem.ItemTemplate><DataTemplate x:DataType="vm:EquipmentInventoryRow"><MenuItem Header="{Binding MenuDisplayName}" FontSize="10" Padding="6,1" MinHeight="0" Margin="0"/></DataTemplate></MenuItem.ItemTemplate></MenuItem>
          <MenuItem Header="Podnieś grupowo" IsVisible="{Binding HasBulkActions}" ItemsSource="{Binding BulkActionsOrEmpty}"><MenuItem.ItemTemplate><DataTemplate x:DataType="equipment:ItemBulkGroup"><MenuItem Header="{Binding DisplayName}" Command="{Binding #EquipmentInventoryPanel.DataContext.ExecuteGroundBulkTakeCommand}" CommandParameter="{Binding}"/></DataTemplate></MenuItem.ItemTemplate></MenuItem>
          <MenuItem Header="Wyjmij" IsVisible="{Binding IsIdentifiedContainer}" Tag="{Binding}" ItemsSource="{Binding ContainerContentsOrEmpty}" Click="TakeGroundContainerItem_OnClick"><MenuItem.ItemTemplate><DataTemplate x:DataType="vm:ContainerInventoryItem"><MenuItem Header="{Binding MenuDisplayName}" FontSize="10" Padding="6,1" MinHeight="0" Margin="0"/></DataTemplate></MenuItem.ItemTemplate></MenuItem>
          <MenuItem Header="Wyjmij grupowo" IsVisible="{Binding HasContainerBulkActions}" Tag="{Binding}" ItemsSource="{Binding ContainerBulkActionsOrEmpty}" Click="TakeGroundContainerItemGroup_OnClick"><MenuItem.ItemTemplate><DataTemplate x:DataType="equipment:ItemBulkGroup"><MenuItem Header="{Binding DisplayName}" FontSize="10" Padding="6,1" MinHeight="0" Margin="0"/></DataTemplate></MenuItem.ItemTemplate></MenuItem>
          <MenuItem Header="Otwórz kluczem" IsVisible="{Binding CanUnlockContainer}" Command="{Binding #EquipmentInventoryPanel.DataContext.ExecuteGroundUnlockCommand}" CommandParameter="{Binding}"/>
          <MenuItem Header="Otwórz" IsVisible="{Binding CanOpenContainer}" Command="{Binding #EquipmentInventoryPanel.DataContext.ExecuteGroundOpenCommand}" CommandParameter="{Binding}"/>
          <MenuItem Header="Zamknij" IsVisible="{Binding CanCloseContainer}" Command="{Binding #EquipmentInventoryPanel.DataContext.ExecuteGroundCloseCommand}" CommandParameter="{Binding}"/>
          <MenuItem Header="Zamknij na klucz" IsVisible="{Binding CanLockContainer}" Command="{Binding #EquipmentInventoryPanel.DataContext.ExecuteGroundLockCommand}" CommandParameter="{Binding}"/>
          <Separator/>
          <MenuItem Header="Wstaw nazwę do linii poleceń" Command="{Binding #EquipmentInventoryPanel.DataContext.InsertEquipmentItemNameToCommandBarCommand}" CommandParameter="{Binding}"/>
        </ContextMenu></Border.ContextMenu><Grid ColumnDefinitions="Auto,Auto,*,Auto,Auto"><TextBlock Text="{Binding ContainerLabel}" Classes="mud-faint" FontSize="10" Margin="0,0,5,0"/><TextBlock Grid.Column="1" Text="{Binding RareLabel}" Classes="mud-status-bad" FontSize="10" Margin="0,0,5,0"/><controls:AnsiTextBlock Grid.Column="2" AnsiText="{Binding Name}" TextWrapping="Wrap" FontSize="12"/><TextBlock Grid.Column="3" Text="{Binding RandomSlotDisplayText}" Classes="mud-faint" FontSize="10" Margin="5,0,0,0" IsVisible="{Binding HasRandomSlotLabel}"/><TextBlock Grid.Column="4" Text="{Binding ContainerAccessLabel}" Classes="mud-faint" FontSize="10" Margin="5,0,0,0"/></Grid></Border>
      </DataTemplate></ItemsControl.ItemTemplate></ItemsControl>
    </StackPanel></ScrollViewer></TabItem>
    <TabItem Header="Tatuaże"><ScrollViewer><StackPanel Margin="6" Spacing="2">
      <TextBlock Text="Tatuaże odczytane z examine self." Classes="mud-faint" FontSize="11"/>
      <ItemsControl ItemsSource="{Binding EquipmentInventory.Tattoos}"><ItemsControl.ItemTemplate><DataTemplate x:DataType="equipment:TattooItem">
        <Border Classes="mud-section" Padding="4" Margin="0,1"><ToolTip.Tip><TextBlock Text="{Binding Description}" TextWrapping="NoWrap"/></ToolTip.Tip><StackPanel Spacing="2"><Grid ColumnDefinitions="110,*"><TextBlock Text="{Binding Location}" Classes="mud-faint" FontSize="11"/><TextBlock Grid.Column="1" Text="{Binding Name}" TextWrapping="Wrap" FontSize="12"/></Grid><TextBlock Text="{Binding Description}" TextWrapping="Wrap" FontSize="11" Margin="110,0,0,0"/></StackPanel></Border>
      </DataTemplate></ItemsControl.ItemTemplate></ItemsControl>
    </StackPanel></ScrollViewer></TabItem>
  </TabControl>
  </Grid>
</UserControl>
